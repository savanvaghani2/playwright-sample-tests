{
  "name": "playwright-js-framework",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",
    "build:test": "NODE_ENV=test vite build",
    "start": "vite",
    "start:test": "NODE_ENV=test vite",
    "test:coverage": "COVERAGE=true npx tdpw test --coverage --project=coverage",
    "test:coverage:low": "COVERAGE=true npx tdpw test --coverage --project=coverage --grep @coverage-low --ci-run-id coverage-low --tags coverage,low",
    "test:coverage:mid": "COVERAGE=true npx tdpw test --coverage --project=coverage --grep @coverage-mid --ci-run-id coverage-mid --tags coverage,mid",
    "test:coverage:high": "COVERAGE=true npx tdpw test --coverage --project=coverage --grep @coverage-high --ci-run-id coverage-high --tags coverage,high",
    "test:coverage:all-profiles": "npm run test:coverage:low && npm run test:coverage:mid && npm run test:coverage:high",
    "test:split": "./scripts/split-local.sh",
    "test:split:overlap": "SPLIT_OVERLAP=1 ./scripts/split-local.sh",
    "test:quota-burn": "./scripts/quota-burn.sh",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test:matrix": "./scripts/matrix-local.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.1.0",
    "dotenv": "^17.4.2",
    "prettier": "3.9.1",
    "vite": "^6.3.5",
    "vite-plugin-istanbul": "^7.0.12"
  },
  "dependencies": {
    "@testdino/playwright": "^2.5.0",
    "playwright": "^1.62.1"
  }
}
